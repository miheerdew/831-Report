\ProvidesPackage{macros}

\RequirePackage{amsmath, amsthm}
\RequirePackage{amsfonts}
\RequirePackage{amssymb}
\RequirePackage{mathtools}
\RequirePackage{xparse}

\NewDocumentCommand{\Ex}{e{_} d[]}{%
  \mathbb{E}\IfNoValueF{#1}{_{#1}}\IfNoValueF{#2}{\left[ #2 \right]}
}%

\NewDocumentCommand{\Pb}{d()}{\mathbb{P}\IfNoValueF{#1}{\left( #1 \right)}}

\NewDocumentCommand{\Ug}{s e{_}}{%
  \IfBooleanTF #1 {%
    \overline{\mathcal{U}}
  }{%
    \mathcal{U}\IfNoValueF{#2}{_{#2}}
  }
}

\NewDocumentCommand{\Lg}{e{_}}{%
  \mathcal{L}\IfNoValueF{#1}{_{#1}}
}

\NewDocumentCommand{\N}{s}{
  \mathbb{N}\IfBooleanT{#1}{^*}
}

\newcommand{\cl}[1]{\mathcal{#1}}
\newcommand{\norm}[1]{\left\lVert#1\right\rVert}
\newcommand{\R}{\mathbb{R}}
\newcommand{\dequal}{\overset{d}{=}}
\newcommand{\dconv}{\xrightarrow{d}}
\newcommand{\pconv}{\xrightarrow{p}}
%\newcommand{\Ex}[1]{\mathbb{E}\left[ #1 \right]}
\newcommand{\set}[1]{\left\{ #1 \right\}}
\newcommand{\pp}{2^{[p]}}
\newcommand{\rangeS}[1]{\emptyset \neq #1 \subset [p]}
\newcommand{\range}[1]{#1 \in [p]}
\DeclareMathOperator{\Var}{Var}
%\newcommand{\defeq}{\vcentcolon=} Use \coloneqq

\newcommand{\cF}{\mathcal{F}}
\newcommand\restr[2]{{% we make the whole thing an ordinary symbol
  \left.\kern-\nulldelimiterspace % automatically resize the bar with \right
  #1 % the function
  \vphantom{\big|} % pretend it's a little taller at normal size
  \right|_{#2} % this is the delimiter
  }}
  
  \newcommand{\Oplus}{\ensuremath{\vcenter{\hbox{\scalebox{1.5}{$\oplus$}}}}}
  

% \renewcommand{\epsilon}{\varepsilon}
% \renewcommand{\theta}{\vartheta}
% \renewcommand{\kappa}{\varkappa}
% \renewcommand{\rho}{\varrho} 
\renewcommand{\phi}{\varphi}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}{Lemma}[theorem]
\newtheorem{cor}{Corollary}[theorem]

\theoremstyle{remark}
\newtheorem*{remark}{Remark}

\theoremstyle{definition}
\newtheorem{example}{Example}

\newcommand{\lemmaautorefname}{Lemma}
\newcommand{\corautorefname}{Corollary}

